{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's public profile.",
      "type": "object",
      "properties": {
        "uid": { "type": "string", "description": "The user's unique ID." },
        "email": { "type": "string", "format": "email", "description": "The user's email address." },
        "displayName": { "type": "string", "description": "The user's display name." },
        "photoURL": { "type": "string", "format": "uri", "description": "URL of the user's profile picture." }
      },
      "required": ["uid", "email"]
    },
    "Product": {
      "title": "Product",
      "description": "Represents a product in the store.",
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "The name of the product." },
        "category": { "type": "string", "description": "The category of the product." },
        "price": { "type": "number", "description": "The price of the product." },
        "description": { "type": "string", "description": "The description of the product." },
        "isTrending": { "type": "boolean", "description": "Whether the product is currently trending." },
        "isNew": { "type": "boolean", "description": "Whether the product is a new arrival." }
      },
      "required": ["name", "category", "price", "description"]
    },
    "ProductVariant": {
      "title": "Product Variant",
      "description": "Represents a specific variant of a product (e.g., by color).",
      "type": "object",
      "properties": {
        "color": { "type": "string", "description": "The color of the product variant." },
        "colorHex": { "type": "string", "description": "The hex code for the variant's color." },
        "imageIds": { "type": "array", "items": { "type": "string" }, "description": "An array of image IDs from the placeholder images." }
      },
      "required": ["color", "colorHex", "imageIds"]
    },
    "ProductReview": {
      "title": "Product Review",
      "description": "A review for a product left by a customer.",
      "type": "object",
      "properties": {
        "author": { "type": "string", "description": "Name of the person who left the review." },
        "rating": { "type": "number", "description": "The rating from 1 to 5." },
        "title": { "type": "string", "description": "The title of the review." },
        "comment": { "type": "string", "description": "The body of the review comment." },
        "date": { "type": "string", "format": "date", "description": "The date the review was submitted." }
      },
      "required": ["author", "rating", "title", "comment", "date"]
    }
  },
  "auth": {
    "providers": ["email_password", "google.com"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/UserProfile" },
      "description": "Stores public information about a user."
    },
    "/products/{productId}": {
      "schema": { "$ref": "#/entities/Product" },
      "description": "Stores core product information."
    },
    "/products/{productId}/variants/{variantId}": {
        "schema": { "$ref": "#/entities/ProductVariant" },
        "description": "Stores all variants for a specific product."
    },
    "/products/{productId}/reviews/{reviewId}": {
        "schema": { "$ref": "#/entities/ProductReview" },
        "description": "Stores all reviews for a specific product."
    }
  }
}
